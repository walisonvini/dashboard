services:
  web:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: dashboard_web
    depends_on:
      - db
    restart: always
    env_file:
      - .env
    networks:
      - dashboard
      - proxy-net
    volumes:
      - dashboard_build_assets:/var/www/html/public/build:rw

  db:
    image: mysql:8.0
    container_name: dashboard_db
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    networks:
      - dashboard

networks:
  dashboard:
  proxy-net:
    external: true

volumes:
  db_data:
  dashboard_build_assets:
    name: dashboard_build_assets